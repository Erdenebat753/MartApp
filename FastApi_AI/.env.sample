OPENAI_API_KEY="키 입력"
OPENAI_MODEL=gpt-4o-mini

# CORS
# - Allow all (JSON array recommended): CORS_ORIGINS=["*"]
# - Specific origins (JSON array): CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
# - Or comma-separated: CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ORIGINS=["*"]

# Admin login (AdminDashboard only)
# Set these on the server; Client app does not use login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
JWT_SECRET=please_change_me
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database connection string
# - Leave unset to keep using the default local SQLite file (`./store_nav.db`)
# - Provide a PostgreSQL URL when deploying (example shown)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/store_nav

# Cloudinary (used for file uploads)
# Create an account at cloudinary.com and set these values
CLOUDINARY_CLOUD_NAME=dki5t8y12
CLOUDINARY_API_KEY=979452458129615
CLOUDINARY_API_SECRET=WF9WbBRCYVJW3FUD9HTou36Gkzs
CLOUDINARY_FOLDER=store-nav
CLOUDINARY_URL=cloudinary://979452458129615:WF9WbBRCYVJW3FUD9HTou36Gkzs@dki5t8y12
