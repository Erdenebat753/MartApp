export const MAP_WIDTH_PX = 675;
export const MAP_HEIGHT_PX = 878;

// Approximate scale: pixels per 1 meter in AR world.
// Calibrate this value for your floorplan.
export const PIXELS_PER_METER = 80;
